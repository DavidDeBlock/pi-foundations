/**
 * Shared barrel — template skeleton for downstream projects.
 *
 * Populate this file with `export * from './validations'` and
 * `export * from './types'` once a new project copies this template
 * and adds its own shared types and Zod schemas.
 */

export {}
