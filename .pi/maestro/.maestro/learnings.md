# Maestro Learnings — maestro

Accumulated learnings from Maestro runs. Each entry is a dated, scoped observation about this repo.

## 2026-06-05 — Issue #283 (success)

- **What worked:** Builder produced a clean repair/completion extraction (6 new files: completion.ts, completion.types.ts, completion.errors.ts, pricing.ts, stock.ts, __tests__/completion.test.ts) with facade re-exports that preserved the 1762-line existing test surface unchanged; The module shape (completion.ts + private types/errors + private pricing/stock ports + facade re-exports from repair.service.ts) is a clean template for extracting the other six sub-domains still hosted in repair.service.ts; Final test run was 1895/1895 passing with the new test file covering the new module's pricing and stock ports in isolation, and the existing integration test for completeRepairPayment still passing through the facade
- **What failed:** The `reviewed` evidence marker is still missing despite reviewer approval on attempt 2, so the close phase warns about missing evidence; The previous retrospective attempt (2026-06-04T23:24:02) failed verdict extraction in the orchestrator — appears to be a parser bug since the session log shows a well-formed PHASE_OUTPUT block was emitted
- **Surprising:** Working memory ships with `files_touched: []` and `test_results: []` despite the test evidence file recording 1895 tests run and the reviewer log showing many real code changes — the retrospective has to reconstruct state from session logs; The `target repo path` field in working memory points to `.pi/maestro` (the harness), but all code work happens in the parent `/home/david/projects/pi-pos-v1` directory — agents consistently `cd ..` before any file operations
- **Repo-specific learnings:** The actual project root is `/home/david/projects/pi-pos-v1` (one level up from the maestro working dir); the maestro harness lives at `.pi/maestro` inside the repo, and all `git`, `pnpm`, and `npx vitest` operations target the parent; Test stack is pnpm + vitest + drizzle-orm + better-sqlite3; full suite is `pnpm test --run` (1895 tests) and the server-only suite is `cd server && npx vitest run` (834 tests including 4 network-gated skips); repair.service.ts was originally 1985 lines and still hosts six other sub-domains (intake, status transitions, line items, workers, history, etc.) that need similar extraction passes; this round only moved completeRepairPayment to repair/completion
## 2026-06-05 — Issue #285 (success)

- **What worked:** Builder followed the AC literally: created the deep-module folder, used TDD (12 focused contract tests covering the 9 AC-listed types + 3 sub-module namespaces), left api.ts untouched via the AC's 'or' path, kept the surface minimal for HITL sign-off, and embedded architect-style structural analysis in the barrel's docstring (affected concepts, structural decision, boundary implications, source-of-truth impact, allowed/forbidden shortcuts); Barrel uses `export * as <name>` namespace re-exports that is forward-compatible with the later slices that will populate adapters/formatters/domain — identical pattern already in catalog, staff, pos features so the convention is consistent; Reviewer on attempt 2 approved after attempt 1 feedback; test_runner on attempt 2 passed all 783 client tests (771 original + 12 new); close phase still recorded success despite the missing-evidence warning
- **What failed:** The `reviewed` evidence marker is still not being written by the reviewer phase despite an `approved` verdict — same gap as issue #283; .maestro/evidence/285/ is empty so close warns about both `tested` and `reviewed`; Scout phase returned status=error (verdict extraction failed in the orchestrator) — appears to be the same parser issue that broke the previous retrospective attempt on 2026-06-04T23:24:02; the agent runs were still able to proceed because builder/reviewer/test_runner were independent
- **Surprising:** All six new files in client/src/features/repairs/repair/ are untracked at session end — reviewer approved but nothing was committed, so the foundation slice is only on the filesystem and will be lost if the working tree is reset; STATUS_COLORS is duplicated in client/src/features/customers/components/CustomerDetailPanel.tsx and client/src/features/staff/components/StaffDetailPage.tsx, and VALID_TRANSITIONS is inlined in RepairDetailPage.tsx — both are documented as later-slice consolidation targets for repair/formatters.ts and repair/domain.ts but the duplication was not flagged by scout; Working memory's files_touched:[] is empty even though 6 new files and 12 new tests were created; the retrospective has to reconstruct the actual work from session logs and git status
- **Repo-specific learnings:** PRD #284 frontend deep-module refactor: issue #285 is the client-side foundation slice (barrel + types re-export + 3 placeholder sub-modules + 12-test contract suite in client/src/features/repairs/repair/); the server-side counterpart was issue #283 (server/src/services/repair/completion.ts). Subsequent PRD #284 slices will migrate api.ts functions into repair/adapters.ts, consolidate formatters into repair/formatters.ts, and move domain constants into repair/domain.ts; Client feature convention: each feature folder (catalog, staff, pos, repairs) keeps a `types.ts` that re-exports from @shared/types/<entity>.js and is the single import path for that feature's types — repairs/ now follows this pattern; repair deep module sits inside the feature folder (client/src/features/repairs/repair/), not at the feature root; Test command for the client slice is `cd client && pnpm test` (vitest, watch mode by default; use `pnpm test --run` for single-shot) which currently runs 783 tests across 65 files; the full repo `pnpm test --run` runs 1895 tests; client ESLint is `cd client && pnpm lint`; `export * as <name>` namespace re-exports in the barrel are the project's forward-compatibility pattern for placeholder sub-modules — the placeholder constant `__<name>_pending_migration = true` makes the file a module so `export *` is meaningful
- **Proposed amendments:** Reviewer phase must write reviewed.json evidence on approved verdict
