What would it take to create a dashboard where I can start and stop the different projects in the projects folder? Lucky I have now saved up tmix sessions where I can quickly restart everything, but yeah, I want something like that. /home/david/projects/pi-foundations/projects



Currently I have several types of projects I need to start or have an easy way to access them, but all access them all from the same dashboard, let's say.
- A4 math is, for now, just a simple HTML page. It can be served with the Python HTTP server, but it can also be shown as an HTML page. For the sake of consistency, let's use the Python HTTP server for now.
- The Cozy Ledger app: same story, the Python HTTP server.
- Magic the Gathering Arena logs: I think that's also the Python HTTP server.
- ZaakOs has the pnpm command. 

And then I have one alt case because this one is in another directory. 
~/projects/pi-pos-v1/ This one is also the PNPM command. 

We have to make sure the ports are not interfering with each other. 